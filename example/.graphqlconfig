{
  "schemaPath": "schema.json",
  "includes": [
    "query.graphql",
    "mutations.graphql"
  ],
  "extensions": {
    "codegen": {
      "target": "flow",
      "schemas": ["schema.graphql"],
      "output": "annotations.flow.js",
      "mergedSchemaOutputPath": "schema.json"
    }
  }
}
